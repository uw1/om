minimal extensible view framework
MutationObserver
ui(nameS, stateO, dataX)
style is customizable as global object
# core
view object
prop prossess
dom transformer

# api eg
[WinBox.js – Modern HTML5 Window Manager](https://nextapps-de.github.io/winbox/)

# api
set
render
	by default n2 provide a real dom createElement, but you are free to replace it with some nonsense framework like react
# 底层js
底层js还是得靠js实现。不方便用lisp
functional (include reactive)
	global/essensial
# ui, dom/event
genElement
genCompound
input(...nessecery, {optional})
num: key, str: parse, obj: option, arr: child
get_type()
error(true, 'message')
	 throw Error('Whoops!')
# declaritive api
backend usable
# evdn
extensible view data notation
h1 title
steost [u test url github.com/test]
setoste [f currentTime]
h1.red red title
list no need to add number	
	must end with empty line

# css
[ben-rogerson/twin.macro: 🦹‍♂️ Twin blends the magic of Tailwind with the flexibility of css-in-js (emotion, styled-components and goober) at build time.](https://github.com/ben-rogerson/twin.macro)

# html level media query
change not only css